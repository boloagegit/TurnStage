## Choose where the profile lives

| Scope | Use it for |
| --- | --- |
| Workspace | Project-specific endpoints, mappings, and UI behavior |
| User | Reusable profiles and environments shared across workspaces |

Profiles are JSONC files. Tokens and API keys belong in VS Code SecretStorage,
not in the profile.
